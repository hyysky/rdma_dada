# pipelinectl

Planned user-facing orchestration application. It will validate topology,
derive ring reader counts and block geometry, create rings, launch configured
applications, and report lifecycle state. It will not contain algorithms.
