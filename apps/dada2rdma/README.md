# dada2rdma

Planned output transport application. It will consume a processed PSRDADA ring,
repacketize records according to the input header and JSON transport settings,
and send them through the RDMA adapter.
