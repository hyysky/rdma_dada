# vdif_unpack

Planned host module that validates raw record geometry, removes the 64-byte
application packet header, and reorders payload samples into the configured
compute layout. The exact payload order remains configuration-defined.
